name: Security Audit

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]
  schedule:
    # Run security audit weekly on Mondays at 9 AM UTC
    - cron: '0 9 * * 1'

concurrency:
  group: security-audit-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  security-audit:
    name: Security Audit
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Run security audit
        shell: bash
        run: |
          set -o pipefail
          yarn audit --level moderate | tee yarn-audit.log

      - name: Run dependency license check
        shell: bash
        run: |
          # Generate license report
          npx --yes license-checker --json --out licenses.json

          # Check for problematic licenses by excluding known problematic ones
          # This approach is more flexible than --onlyAllow
          npx --yes license-checker --failOn 'GPL-3.0;AGPL-3.0;GPL-2.0;LGPL-2.1;LGPL-2.0;GPL-1.0;AGPL-1.0;SSPL-1.0;OSL-3.0;EPL-1.0;EPL-2.0;CDDL-1.0;CDDL-1.1' || {
            echo "❌ Problematic licenses detected!"
            echo "📋 Generating detailed license report..."
            npx --yes license-checker --summary
            exit 1
          }

          echo "✅ License check passed - no problematic licenses found"
          echo "📊 License summary:"
          npx --yes license-checker --summary

      - name: Upload security audit results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: security-audit-results
          path: |
            yarn-audit.log
            licenses.json
          if-no-files-found: warn
          retention-days: 30
